# Contributing

- Small commits, one concern per PR.
- `pnpm vitest run` and `pnpm typecheck` must pass.
- Infra changes belong to the CI-agent persona (`.github/`, `infra/`).
- Reference the ticket id (ENT-xxx) in feature commits.
