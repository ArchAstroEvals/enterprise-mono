# enterprise-mono

pnpm monorepo: SaaS platform across `apps/*` and `packages/*`.

## Workspaces

- `packages/auth`, `packages/billing`, `packages/teams`, `packages/flags`
- `apps/api`, `apps/web`

## Test

`pnpm vitest run` from the root.
