# @mono/api

Node HTTP API over the workspace packages.

Routes live in `src/routes`, wired in `src/app.ts`.
Integration tests (`*.int.test.ts`) boot the app on an ephemeral port.
Service version lives in `src/version.ts`.
