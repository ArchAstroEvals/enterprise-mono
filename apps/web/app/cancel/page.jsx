export const metadata = { title: "Cancel — Mono Web" };

export default function Cancel() {
  return (
    <div>
      <h1>Cancel</h1>
      <p>Plans stay active until the period ends.</p>
    </div>
  );
}
